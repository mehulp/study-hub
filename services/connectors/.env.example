DATABASE_URL=postgresql+psycopg://study_hub:study_hub_dev_password@localhost:5432/study_hub
AUTH_SERVICE_URL=http://localhost:8001
ITEMS_SERVICE_URL=http://localhost:8002
OAUTH_CLIENT_ID=connectors-service
OAUTH_CLIENT_SECRET=changeme
# Required for the app to import at all (app/twitter_client.py reads these
# at module load) even though the Twitter connector is unused (Decision
# #61) -- blank is fine unless you're actually testing the OAuth flow.
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=
TWITTER_REDIRECT_URI=
