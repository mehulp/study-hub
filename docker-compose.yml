services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: bookmarks_hub
      POSTGRES_PASSWORD: bookmarks_hub_dev_password
      POSTGRES_DB: bookmarks_hub
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
